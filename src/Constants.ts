// Logging level
export const ERR: boolean = true;
export const LOG: boolean = true;

// Helpful stuff
export const ios: boolean = (require('react-native').Platform.OS === 'ios');
export const android: boolean = (require('react-native').Platform.OS === 'android');

// haha
export const lmao: number = 1000;
